interface:
  display_name: "Research Vault"
  short_description: "Build an evidence-backed, agent-native research vault"
  default_prompt: "Use $research-vault to discover, acquire, parse, and synthesize an Obsidian-compatible research vault with claim-level provenance and a derived agent index."
